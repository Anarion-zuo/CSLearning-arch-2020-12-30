//
// Created by anarion on 6/13/19.
//

#include "dynamic.h"


long LIS(std::vector<long>& nums){
    long size = nums.size();
    std::vector<long> lis(size, 0);
    lis[0] = 1;
    for (size_t i = 0; i < size; ++i){

    }
}